$(function() {

  $('.postbtn-link').on('click', function() {
    $('.overlay, .modalWindow').fadeIn();
  });

  $('.overlay').on('click', function() {
    $('.overlay, .modalWindow').fadeOut();
  });

  locateCenter();
  $(window).resize(locateCenter);

  function locateCenter() {
    let w = $(window).width();
    let h = $(window).height();

    let cw = $('.modalWindow').outerWidth();
    let ch = $('.modalWindow').outerHeight();

    $('.modalWindow').css({
      'left': ((w - cw) / 2) + 'px',
      'top': ((h - ch) / 2) + 'px'
    });
  }
});

$(function() {

  $('.fas.fa-heart').on('click', function() {
    $('.overlay, .modalWindow').fadeIn();
  });

  $('.overlay').on('click', function() {
    $('.overlay, .modalWindow').fadeOut();
  });

  locateCenter();
  $(window).resize(locateCenter);

  function locateCenter() {
    let w = $(window).width();
    let h = $(window).height();

    let cw = $('.modalWindow').outerWidth();
    let ch = $('.modalWindow').outerHeight();

    $('.modalWindow').css({
      'left': ((w - cw) / 2) + 'px',
      'top': ((h - ch) / 2) + 'px'
    });
  }
});

$(function() {

  $('.postsbtn-link').on('click', function() {
    $('.overlay, .modalWindow').fadeIn();
  });

  $('.overlay').on('click', function() {
    $('.overlay, .modalWindow').fadeOut();
  });

  locateCenter();
  $(window).resize(locateCenter);

  function locateCenter() {
    let w = $(window).width();
    let h = $(window).height();

    let cw = $('.modalWindow').outerWidth();
    let ch = $('.modalWindow').outerHeight();

    $('.modalWindow').css({
      'left': ((w - cw) / 2) + 'px',
      'top': ((h - ch) / 2) + 'px'
    });
  }
});

$(function() {

  $('.link-login').on('click', function() {
    $('.overlay, .modalWindow').fadeIn();
  });

  $('.overlay').on('click', function() {
    $('.overlay, .modalWindow').fadeOut();
  });

  locateCenter();
  $(window).resize(locateCenter);

  function locateCenter() {
    let w = $(window).width();
    let h = $(window).height();

    let cw = $('.modalWindow').outerWidth();
    let ch = $('.modalWindow').outerHeight();

    $('.modalWindow').css({
      'left': ((w - cw) / 2) + 'px',
      'top': ((h - ch) / 2) + 'px'
    });
  }
});

$(function() {

  $('.user-menu__text').on('click', function() {
    $('.overlay, .modalWindow').fadeIn();
  });

  $('.overlay').on('click', function() {
    $('.overlay, .modalWindow').fadeOut();
  });

  locateCenter();
  $(window).resize(locateCenter);

  function locateCenter() {
    let w = $(window).width();
    let h = $(window).height();

    let cw = $('.modalWindow').outerWidth();
    let ch = $('.modalWindow').outerHeight();

    $('.modalWindow').css({
      'left': ((w - cw) / 2) + 'px',
      'top': ((h - ch) / 2) + 'px'
    });
  }
});

$(function() {

  $('.link-logins').on('click', function() {
    $('.overlay, .modalWindow').fadeIn();
  });

  $('.overlay').on('click', function() {
    $('.overlay, .modalWindow').fadeOut();
  });

  locateCenter();
  $(window).resize(locateCenter);

  function locateCenter() {
    let w = $(window).width();
    let h = $(window).height();

    let cw = $('.modalWindow').outerWidth();
    let ch = $('.modalWindow').outerHeight();

    $('.modalWindow').css({
      'left': ((w - cw) / 2) + 'px',
      'top': ((h - ch) / 2) + 'px'
    });
  }
});

// $(function() {

//   $('.user-menu__target').on('click', function() {
//     $('.overlay, .modalWindow').fadeIn();
//   });

//   $('.overlay').on('click', function() {
//     $('.overlay, .modalWindow').fadeOut();
//   });

//   locateCenter();
//   $(window).resize(locateCenter);

//   function locateCenter() {
//     let w = $(window).width();
//     let h = $(window).height();

//     let cw = $('.modalWindow').outerWidth();
//     let ch = $('.modalWindow').outerHeight();

//     $('.modalWindow').css({
//       'left': ((w - cw) / 2) + 'px',
//       'top': ((h - ch) / 2) + 'px'
//     });
//   }
// });

// $(function() {

//   $('.fa.fa-heart').on('click', function() {
//     $('.overlay, .modalWindow').fadeIn();
//   });

//   $('.overlay').on('click', function() {
//     $('.overlay, .modalWindow').fadeOut();
//   });

//   locateCenter();
//   $(window).resize(locateCenter);

//   function locateCenter() {
//     let w = $(window).width();
//     let h = $(window).height();

//     let cw = $('.modalWindow').outerWidth();
//     let ch = $('.modalWindow').outerHeight();

//     $('.modalWindow').css({
//       'left': ((w - cw) / 2) + 'px',
//       'top': ((h - ch) / 2) + 'px'
//     });
//   }
// });
